name: C++ CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4


      - name: Install Compiler
        run: sudo apt-get update


      - name: Verify Build
        run: echo "Pipeline Build Successful"
